# Secrets
.env
.env.*
!.env.example

# Personal profile (PII) — keep your real resume and profile out of git.
# The *.example.* templates ARE committed.
profile/profile.yaml
profile/resume_master.md
profile/star_stories.csv
profile/projects/

# Machine-owned app state written by the setup wizard. Only toggles today, but
# it should never show up dirty in git status or be committable by accident.
settings.json

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Local data / generated artifacts
jobpilot.db
output/
dist/
